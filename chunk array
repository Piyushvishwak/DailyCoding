/**
 * @param {Array} arr
 * @param {number} size
 * @return {Array}
 */
var chunk = function(arr, size) {
    var arr2=[];
    for(var i=0;i<arr.length;i+=size){
        arr2.push(arr.slice(i,size+i));
    }
    return arr2;
};
